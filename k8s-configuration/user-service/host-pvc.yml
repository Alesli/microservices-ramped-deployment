piVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-host-pvc
spec:
  volumeName: user-host-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: hostpath
  resources:
    requests:
      storage: 1Gi